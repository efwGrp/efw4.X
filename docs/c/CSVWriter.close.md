# CSVWriter.close

该函数用于关闭 Java 写入器。

## 示例

```javascript
var csvWriter = new CSVWriter("output/test.txt", ",", "\"", "MS932");
...
csvWriter.close();
```

## API

| 调用 | 返回值 |
|---|---|
| `csvWriter. close ( )` | `void` |