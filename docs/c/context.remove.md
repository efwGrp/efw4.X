# context.remove

`remove` 函数从上下文中删除数据。

## 示例

```javascript
context.remove("BROADCAST_CONTENT");
```
## API

| 调用 | 返回值 |
|---|---|
| `context. remove ( key )` | `void` |

| 参数 | 类型 | 描述 |
|---|---|---|
| `key` | `String` | 用于标识上下文信息的键。 |
