# context.remove

`remove` 関数は、コンテキストからデータを削除します。

## サンプル

```javascript
context.remove("BROADCAST_CONTENT");
```
## API

| 呼び出し | 戻り値 |
|---|---|
| `context. remove ( key )` | `void` |

| パラメータ | 型 | 説明 |
|---|---|---|
| `key` | `String` | コンテキスト情報を識別するために使用するキー。 |
