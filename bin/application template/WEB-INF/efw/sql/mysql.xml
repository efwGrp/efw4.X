<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqls>
<sqls>
<!--
	<sql id="sel汎用マスター一覧">
		SELECT
			t.dep_id,						-- 部門種別ID
			t.master_type,					-- 汎用マスター区分
			t.general_id,					-- 汎用ID
			t.master_nm,					-- 項目名
			t.comment						-- コメント
        FROM @schema.m_general t
        WHERE 1=1
       	AND t.dep_id = :dep_id
       	AND t.master_type = :master_type
		<if exists="master_nm">
			AND t.master_nm = :master_nm
		</if>
        ORDER BY sort_no
	</sql>
	<sql id="sel一覧検索cnt">
		SELECT count(*) AS cnt
		<include groupId="MS06" sqlId="sel一覧検索sub"/>
	</sql>
-->
</sqls>
