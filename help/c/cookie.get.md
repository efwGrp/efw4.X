# cookie.get

`get` 函数用于从 cookie 中获取数据。

## 示例

```javascript
var userId = cookie.get("USER_ID");
```

## API

| 调用 | 返回值 |
|---|---|
| `cookie. get ( key )` | `{String}` |

| 参数 | 类型 | 描述 |
|---|---|---|
| `key` | `String` | 用于标识 cookie 信息。 |